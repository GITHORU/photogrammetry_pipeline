# PhotoGeoAlign - Dépendances Rocky Linux 8 / RHEL 8.2
# Versions optimisées pour libgfortran 5.0 et GLIBC 2.28

PySide6>=6.0.0
# NumPy version compatible RHEL 8.2/libgfortran (évite ELF alignment error)
numpy>=1.18.0,<1.19.0
pyproj>=3.0.0
scipy>=1.7.0
Pillow>=8.0.0
rasterio>=1.3.0
open3d>=0.17.0

# PyInstaller compatible Python 3.6
pyinstaller>=4.10,<5.0.0

# Tests et développement  
pytest>=7.0.0
pytest-qt>=4.0.0

# Note: Les modules argparse, logging, subprocess, os, sys, time, re, pathlib, multiprocessing
# font partie de la bibliothèque standard Python (>=3.6)
